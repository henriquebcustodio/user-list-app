import Card from '../UI/Card';
import classes from './UserList.module.css';

const UserList = props => {
    return (
        <Card className={classes.input}>User List</Card>
    );
};

export default UserList;